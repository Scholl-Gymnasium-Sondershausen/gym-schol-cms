{
  "defaultConnection": "default",
  "connections": {
    "default": {
      "connector": "mongoose",
      "settings": {
        "srv": false,
        "host": "${process.env.DATABASE_HOST || 'schollgym-shard-00-00-7heei.mongodb.net,schollgym-shard-00-01-7heei.mongodb.net,schollgym-shard-00-02-7heei.mongodb.net'}",
        "port": "${process.env.DATABASE_PORT || 27017}",
        "database": "${process.env.DATABASE_NAME || 'SchollGymProd'}",
        "username": "${process.env.DATABASE_USERNAME || 'schollDbProd'}",
        "password": "${process.env.DATABASE_PASSWORD || 'hnzEapEU6RcrPhtm4'}"
      },
      "options": {
        "ssl": true
      }
    }
  }
}

